# Grayson Byrd's Neovim Config

Clone this repo into your ~/.config directory.

Ensure Neovim is installed on your system.

Type:
```bash
nvim
```
into a terminal.

Watch the magic happens as all plugins are automatically downloaded and configured.
